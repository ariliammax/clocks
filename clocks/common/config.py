# config.py
# in clocks.common


class Config:
    INT_LEN = 8
    # (HOST, PORT)
    MACHINES = [('localhost', 11119),
                ('localhost', 22229),
                ('localhost', 33339)]
    TIMEOUT = 1

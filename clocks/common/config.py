# config.py
# in clocks.common


class Config:
    INT_LEN = 8
    # (HOST, PORT)
    MACHINES = [('localhost', 11134),
                ('localhost', 22212),
                ('localhost', 33323)]
    TIMEOUT = 1
